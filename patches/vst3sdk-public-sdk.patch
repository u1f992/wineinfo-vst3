diff --git a/samples/vst-hosting/validator/CMakeLists.txt b/samples/vst-hosting/validator/CMakeLists.txt
index 3e518fc..61312df 100644
--- a/samples/vst-hosting/validator/CMakeLists.txt
+++ b/samples/vst-hosting/validator/CMakeLists.txt
@@ -130,6 +130,10 @@ target_link_libraries(${target}
     PRIVATE
         sdk_hosting
 )
+if(MINGW)
+    # validator uses wmain() (Unicode entry point), need -municode
+    target_link_options(${target} PRIVATE -municode)
+endif()
 smtg_target_codesign(${target})
 smtg_target_setup_universal_binary(${target})
 if(APPLE AND NOT XCODE)
@@ -154,6 +158,6 @@ if(SMTG_RUN_VST_VALIDATOR)
         )
 	endif()
     add_custom_command(TARGET ${target} POST_BUILD
-	    COMMAND $<TARGET_FILE:${target}> "-selftest"
+	    COMMAND ${CMAKE_CROSSCOMPILING_EMULATOR} $<TARGET_FILE:${target}> "-selftest"
     )
 endif(SMTG_RUN_VST_VALIDATOR)
